<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adrien.madrental">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BookingsActivity"
            android:label="Mes Réservations" />
        <activity
            android:name=".ProfileActivity"
            android:label="Mon Profil" />
        <activity
            android:name=".SearchesActivity"
            android:label="Recherche de véhicules" />
        <activity
            android:name=".BookingState1"
            android:label="Réservation - étape 1 / 2" />
        <activity
            android:name=".BookingState2"
            android:label="Réservation - étape 2 / 2" />
    </application>

</manifest>